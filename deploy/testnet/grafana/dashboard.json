{
  "annotations": {"list": []},
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "type": "timeseries",
      "title": "API Requests by code (rps)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "sum by (code)(rate(api_requests_total[5m]))", "legendFormat": "code={{code}}"}
      ],
      "gridPos": {"x": 0, "y": 0, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "API Latency avg (ms)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "sum(rate(api_latency_ms_sum[5m])) / sum(rate(api_latency_ms_count[5m]))", "legendFormat": "avg"}
      ],
      "gridPos": {"x": 12, "y": 0, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "Service op avg (ms) by service",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "sum by (service)(rate(service_op_ms_sum[5m])) / sum by (service)(rate(service_op_ms_count[5m]))", "legendFormat": "{{service}}"}
      ],
      "gridPos": {"x": 0, "y": 7, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "Consensus proc avg (ms) by kind",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "sum by (kind)(rate(consensus_proc_ms_sum[5m])) / sum by (kind)(rate(consensus_proc_ms_count[5m]))", "legendFormat": "{{kind}}"}
      ],
      "gridPos": {"x": 12, "y": 7, "w": 12, "h": 7}
    },
    {
      "type": "stat",
      "title": "P2P conns open",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "p2p_conns_open", "legendFormat": "open"}
      ],
      "gridPos": {"x": 0, "y": 14, "w": 6, "h": 6}
    },
    {
      "type": "timeseries",
      "title": "P2P conn attempts by result (rps)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "sum by (result)(rate(p2p_conn_attempts_total[5m]))", "legendFormat": "{{result}}"}
      ],
      "gridPos": {"x": 6, "y": 14, "w": 18, "h": 6}
    },
    {
      "type": "timeseries",
      "title": "QBFT verify results (rps)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "sum by (result)(rate(qbft_msg_verified_total[5m]))", "legendFormat": "{{result}}"}
      ],
      "gridPos": {"x": 0, "y": 20, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "QBFT state transitions by type (rps)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "sum by (type)(rate(qbft_state_transitions_total[5m]))", "legendFormat": "{{type}}"}
      ],
      "gridPos": {"x": 12, "y": 20, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "State persist errors (rps)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "rate(state_persist_errors_total[5m])", "legendFormat": "errors"}
      ],
      "gridPos": {"x": 0, "y": 27, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "TSS sessions by result (rps)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "sum by (result)(rate(tss_sessions_total[5m]))", "legendFormat": "{{result}}"}
      ],
      "gridPos": {"x": 12, "y": 27, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "TSS limiter & sessions open",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {"expr": "sum by (kind)(rate(tss_rate_limited_total[5m]))", "legendFormat": "limited {{kind}}"},
        {"expr": "tss_sessions_open", "legendFormat": "open"}
      ],
      "gridPos": {"x": 0, "y": 34, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "DFBA solve avg (ms)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {
          "expr": "sum(rate(dfba_solve_ms_sum[5m])) / sum(rate(dfba_solve_ms_count[5m]))",
          "legendFormat": "avg"
        }
      ],
      "gridPos": {"x": 12, "y": 34, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "DFBA solves by result (rps)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {
          "expr": "sum by (result)(rate(dfba_solve_total[5m]))",
          "legendFormat": "{{result}}"
        }
      ],
      "gridPos": {"x": 0, "y": 41, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "DFBA flows accepted/rejected (rps)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {
          "expr": "sum by (flow)(rate(dfba_accepted_total[5m]))",
          "legendFormat": "accepted {{flow}}"
        },
        {
          "expr": "sum by (flow,reason)(rate(dfba_rejected_total[5m]))",
          "legendFormat": "rejected {{flow}} {{reason}}"
        }
      ],
      "gridPos": {"x": 12, "y": 41, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "DFBA clearing price",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {
          "expr": "dfba_clearing_price",
          "legendFormat": "clearing_price"
        }
      ],
      "gridPos": {"x": 0, "y": 48, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "Block value (bids vs fees)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {
          "expr": "sum(rate(block_value_bids_sum[5m])) / sum(rate(block_value_bids_count[5m]))",
          "legendFormat": "bids_avg"
        },
        {
          "expr": "sum(rate(block_value_fees_sum[5m])) / sum(rate(block_value_fees_count[5m]))",
          "legendFormat": "fees_avg"
        }
      ],
      "gridPos": {"x": 12, "y": 48, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "BEAST decrypt results (rps)",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {
          "expr": "sum by (result)(rate(beast_decrypt_total[5m]))",
          "legendFormat": "{{result}}"
        }
      ],
      "gridPos": {"x": 0, "y": 55, "w": 12, "h": 7}
    },
    {
      "type": "timeseries",
      "title": "Private pool intake (rps) & size",
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "targets": [
        {
          "expr": "sum by (result)(rate(private_pool_in_total[5m]))",
          "legendFormat": "in {{result}}"
        },
        {
          "expr": "private_pool_size",
          "legendFormat": "size"
        }
      ],
      "gridPos": {"x": 12, "y": 55, "w": 12, "h": 7}
    }
  ],
  "refresh": "30s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["aequa", "testnet"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "timezone": "",
  "title": "Aequa Testnet Overview",
  "uid": null,
  "version": 1,
  "weekStart": ""
}
